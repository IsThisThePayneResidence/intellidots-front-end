import { React, Component } from './../../../../viewUtils'


export default class TotalRow extends Component<any, any> {
  
  // get getTotalInfo() {
  //   let total = this.props.totalItemsCount || null
  //   let begin = (this.props.activePage - 1) * this.props.pageSize + 1
  //   let end = this.props.activePage * this.props.pageSize
  //   if (total && total < end ) {
  //     end = total
  //   }
  //   return total ? (
  //     <div className="selected-info">
  //       {`${begin} - ${end} ${locale.from} ${total}`}
  //     </div>
  //   ) : null
  // }
  
  render() {
    return (
      <div className="f-row total-row">
        value isNan
          ? empty
          : cells with total info
      </div>
    )
  }
}

